function weight = HammingWeight(c)
    % Función que calcula el peso de hamming de una palabra de codigo c
    
    % Itera sobre la pal
    weight = 0;
    for i = 1:size(c,1)
        weight = weight + (c(i)-'0');
    end
end